.git/
Dockerfile
.dockerignore

# Build and test artifacts
todo-cli
todo
coverage.*
*.out
*.coverprofile
profile.cov

# Local databases must never be baked into an image
*.db

# Documentation and local tooling
AGENTS.md
README.md
LICENSE
Taskfile.yml

# Flutter mobile app (not consumed by the Go server build)
mobile/

# Cloud Build config (CI-only, not needed in image)
cloudbuild.yaml

# IDE / editor cruft
.idea/
.metadata

# Go coverage reports (regenerable, not needed in image)
coverage.html
coverage.out
